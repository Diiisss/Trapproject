import React from 'react' // el de la profesora no tenia import

const Error = ({mensaje,componente}) => {
                    console.log(props)
  return (
    <div>componente()</div>
  )
}

export default Error